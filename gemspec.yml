name: mdp
summary: "MajorDomoProtocol implementation"
description: "A ruby implementation of the MDP as described in the 0mq guide."
license: MIT
authors: pts
homepage: http://rubygems.org/gems/mdp

dependencies:
  ffi-rzmq: ~> 0.9.0
  
development_dependencies:
  yard: ~> 0.7
  bundler: ~> 1.0.0
